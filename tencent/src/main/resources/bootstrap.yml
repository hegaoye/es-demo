spring:
  application:
    name: tencent-api
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST}
        group: DEFAULT_GROUP
        namespace: ${NAMESPACE}
      config:
        server-addr: ${REGISTER_HOST}
        group: DEFAULT_GROUP
        name: ${CONFIG_NAME:tencent}
        file-extension: yml
        namespace: ${NAMESPACE}

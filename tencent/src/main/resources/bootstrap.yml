spring:
  application:
    name: tencent-api
    jackson:
      date-format: yyyy-MM-dd HH:mm:ss
      time-zone: Asia/Shanghai
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:nacos.public-prod}:${REGISTER_PORT:8848}
        group: DEFAULT_GROUP
        namespace: ${NAMESPACE:05038403-dd3c-45ed-99e9-00cb13facde3}
      config:
        server-addr: ${REGISTER_HOST:nacos.public-prod}:${REGISTER_PORT:8848}
        group: DEFAULT_GROUP
        name: ${CONFIG_NAME:tencent}
        file-extension: yml
        namespace: ${NAMESPACE:05038403-dd3c-45ed-99e9-00cb13facde3}
